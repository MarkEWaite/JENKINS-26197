<?xml version="1.0"?>
<project name="JENKINS-26197" default="version" basedir=".">
  <!-- Adapted from https://gist.github.com/davejlong/874521 -->

  <tstamp>
    <format property="date-time" pattern="MM/dd/yyyy hh:mm:ss aa"/>
    <format property="branch-name" pattern="MM-dd-yyyy-hh-mm-ss-aa"/>
  </tstamp>

  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

  <target name="version" description="Commits all changes to version git">
    <property name="commit-message" value="Committing latest changes at ${date-time}" />

    <echo message="Commiting all changes with message ${commit-message}" />
    <git command="checkout">
      <args>
        <arg value="-b" />
        <arg value="${branch-name}" />
        <arg value="origin/master" />
      </args>
    </git>
    <echo file="${branch-name}" message="This file is on a branch named ${branch-name}"/>
    <git command="add">
      <args>
        <arg value="." />
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-a" />
        <arg value="-m" />
        <arg value="${commit-message}" />
      </args>
    </git>
    <git command="push" />
  </target>
</project>
